# Test requirements inherit the full runtime dependencies so the test
# environment always matches what the code imports at settings-load time.
# The previous minimal list was missing channels, celery, dj-database-url,
# ephem, djangorestframework-simplejwt, drf-spectacular, pytest-cov, etc.
-r requirements.txt
